[user]
	email = tib@tibux.org
	name = tibux
[color]
	ui = true
[push]
	default = simple
[alias]
	up = "!git remote update -p; git merge --ff-only @{u}"
	# ci = commit
	# co = checkout
	# st = status
	# br = branch
[pull]
	ff = only
[core]
	excludesfile = $/.gitignore_global
	# Fix esc in the terminal
	pager = less -R
